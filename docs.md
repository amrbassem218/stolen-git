## Staging

- Get the file hash
- go to the router
- check if a route exists (the key is the hash and the value being the path to a file holding the latest committed version of that file)
- if no route it creates a file with UUID and the file extension of the original file and it creates a route for future reference
-
